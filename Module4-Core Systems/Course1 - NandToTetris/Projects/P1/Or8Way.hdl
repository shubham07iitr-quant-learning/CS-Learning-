// This file is part of www.nand2tetris.org
// and the book "The Elements of Computing Systems"
// by Nisan and Schocken, MIT Press.
// File name: projects/1/Or8Way.hdl
/**
 * 8-way Or gate: 
 * out = in[0] Or in[1] Or ... Or in[7]
 */
CHIP Or8Way {
    IN in[8];
    OUT out;

    PARTS:
    //// Replace this comment with your code.
    //Here we will have to create 7 smaller gates one by one, and send output of 1 as input of another 
    Or(a= in[0], b= in[1], out= Or1); //1st OR gate
    Or(a= in[2] , b= Or1, out= Or2 ); //2nd Or gate
    Or(a= in[3], b= Or2 , out= Or3); //3rd OR gate 
    Or(a= in[4], b= Or3 , out= Or4); //4th OR gate 
    Or(a= in[5], b= Or4 , out= Or5); //5th OR gate 
    Or(a= in[6], b= Or5 , out= Or6); //6th OR gate 
    Or(a= in[7], b= Or6 , out= out); //Last Or gate
}